services:
  horse_db:
    container_name: horse_market_db
    image: postgres

    environment:
      POSTGRES_DB: "market"
      POSTGRES_USER: "market_user"
      POSTGRES_PASSWORD: "1234"

    ports:
      - "5432:5432"
