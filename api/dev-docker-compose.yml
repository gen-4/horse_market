services:
  horse_db:
    container_name: horse_exchange_db
    image: postgres

    environment:
      POSTGRES_DB: "exchange"
      POSTGRES_USER: "exchange_user"
      POSTGRES_PASSWORD: "1234"

    ports:
      - "5432:5432"
